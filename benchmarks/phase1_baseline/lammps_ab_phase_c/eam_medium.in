# Phase C LAMMPS A/B: EAM (Mishin Cu), medium (32000-atom Cu FCC).

units           metal
atom_style      atomic
boundary        p p p

read_data       ../medium.data

pair_style      eam/alloy
pair_coeff      * * ../../../tests/data/Cu_mishin1.eam.alloy Cu

neighbor        1.0 bin
neigh_modify    every 10 delay 0 check no

velocity        all create 300.0 42 mom yes rot no

fix             1 all nve
timestep        0.001

thermo          100
thermo_style    custom step temp pe ke etotal

run             100

reset_timestep  0
run             1000
